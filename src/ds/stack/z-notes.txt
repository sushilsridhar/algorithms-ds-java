Stack is abstract data type,

abstract - meaning implementation details is hidden or not considered

2 major operation
push and pop

insertion and extraction done at one end only
last in first out

real life examples :  web browser back and forward button, function call stack, undo redo in notes

Stack in java internally use Vector which is synchronised dynamic array
Vector is used by Stack because, Stack and Vector are legacy classes created before Arraylist
